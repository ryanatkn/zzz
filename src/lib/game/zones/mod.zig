pub const zone_manager = @import("zone_manager.zig");

// Re-export key types
pub const ZoneManager = zone_manager.ZoneManager;
pub const ZoneMetadata = zone_manager.ZoneMetadata;
pub const ZoneTravelResult = zone_manager.ZoneTravelResult;
pub const CameraMode = zone_manager.CameraMode;
pub const ZoneConfig = zone_manager.ZoneConfig;
