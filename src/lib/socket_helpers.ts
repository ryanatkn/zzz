// Socket connection parameters
export const DEFAULT_HEARTBEAT_INTERVAL = 300000; // 5 minutes
export const DEFAULT_RECONNECT_DELAY = 1000;
export const DEFAULT_RECONNECT_DELAY_MAX = 60000; // 1 minute
export const DEFAULT_AUTO_RECONNECT = true;

// Message handling defaults
export const DEFAULT_RETRY_COUNT = 3;

// WebSocket protocol and connection settings
export const DEFAULT_CLOSE_CODE = 1000; // Normal closure
