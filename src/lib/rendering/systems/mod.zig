// High-level rendering systems and batch renderers

pub const entity_renderer = @import("entity_renderer.zig");

// Re-export main types for convenience
pub const EntityBatchRenderer = entity_renderer.EntityBatchRenderer;
