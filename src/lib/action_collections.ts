// generated by src/lib/action_collections.gen.ts - DO NOT EDIT OR RISK LOST DATA

import {z} from 'zod';
import {
	update_diskfile_action_message,
	delete_diskfile_action_message,
	create_directory_action_message,
	send_prompt_action_message,
	ping_action_message,
	pong_action_message,
	load_session_action_message,
	loaded_session_action_message,
	filer_change_action_message,
	completion_response_action_message,
} from '$lib/action_messages.js';

/**
 * Set of client-only actions.
 */
export const Action_Client = z.discriminatedUnion('method', [
	update_diskfile_action_message,
	delete_diskfile_action_message,
	create_directory_action_message,
	send_prompt_action_message,
]);
export type Action_Client = z.infer<typeof Action_Client>;

/**
 * Set of server-only actions.
 */
export const Action_Server = z.discriminatedUnion('method', [
	ping_action_message,
	pong_action_message,
	load_session_action_message,
	loaded_session_action_message,
	filer_change_action_message,
	completion_response_action_message,
]);
export type Action_Server = z.infer<typeof Action_Server>;

/**
 * All action types combined.
 */
export const Action_Any = z.discriminatedUnion('method', [
	update_diskfile_action_message,
	delete_diskfile_action_message,
	create_directory_action_message,
	send_prompt_action_message,
	ping_action_message,
	pong_action_message,
	load_session_action_message,
	loaded_session_action_message,
	filer_change_action_message,
	completion_response_action_message,
]);
export type Action_Any = z.infer<typeof Action_Any>;

// Maps action methods to their directions ('client', 'server', or 'both')
export const action_directions: Record<string, 'client' | 'server' | 'both'> = {
	update_diskfile: 'client',
	delete_diskfile: 'client',
	create_directory: 'client',
	send_prompt: 'client',
	ping: 'server',
	pong: 'server',
	load_session: 'server',
	loaded_session: 'server',
	filer_change: 'server',
	completion_response: 'server',
};

// generated by src/lib/action_collections.gen.ts - DO NOT EDIT OR RISK LOST DATA
