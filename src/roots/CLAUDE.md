# src/roots

The src/roots directory has the application GUI roots.
Each of its directories is like the `src/routes` of a SvelteKit application,
containing layouts and pages.
The src/roots directory itself may not have any layouts or pages, to avoid confusion,
but roots can depend on each other.